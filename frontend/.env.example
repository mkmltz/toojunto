# Development (optional because the local URL is the safe development fallback)
VITE_API_URL=http://127.0.0.1:8000

# Production build (required)
# VITE_API_URL=/api

# VITE_* values are embedded into the frontend bundle at build time.
